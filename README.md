# 乡链 - 进销存管理系统

一个基于 Spring Boot + Vue 的进销存管理系统。

## 技术栈

### 后端

- Java 17
- Spring Boot 3.2.0
- MyBatis + PageHelper
- MySQL 5.7+
- JWT 认证

### 前端

- Vue 3.5
- Element Plus
- Vite
- Pinia
- Vue Router

## 环境要求

- JDK 17+
- Maven 3.6+
- MySQL 5.7+
- Node.js 18+

## 快速开始

### 1. 数据库初始化

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE testdb CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

# 导入 SQL 文件
source database/testdb.sql
```

### 2. 后端配置

配置文件位于 `src/main/resources/application-dev.properties`：

```properties
# 数据库连接（根据实际情况修改）
spring.datasource.url=jdbc:mysql://localhost:3306/testdb
spring.datasource.username=root
spring.datasource.password=root

# JWT 密钥（生产环境请修改）
config.jwt.secret=YourSecureSecretKey
```

### 3. 启动后端

```bash
# 在项目根目录执行
./mvnw spring-boot:run

# 或打包后运行
./mvnw clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
```

后端服务默认运行在 http://localhost:8080

### 4. 启动前端

```bash
cd Vue

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端服务默认运行在 http://localhost:5173

## 默认账号

| 角色     | 用户名 | 密码   |
| -------- | ------ | ------ |
| 管理员   | admin  | 123456 |
| 测试用户 | test   | test   |

## 项目结构

```
├── Vue/                      # Vue 前端项目
│   ├── src/
│   │   ├── components/       # 组件目录
│   │   │   ├── base/        # 基础数据管理（商品、客户、仓库等）
│   │   │   ├── inventory/   # 库存管理
│   │   │   ├── menu/       # 菜单组件
│   │   │   ├── notice/    # 公告管理
│   │   │   ├── purchase/   # 采购管理
│   │   │   ├── sales/     # 销售管理
│   │   │   └── system/    # 系统管理
│   │   ├── router/          # 路由配置
│   │   ├── store/          # Pinia 状态管理
│   │   ├── utils/          # 工具函数
│   │   └── views/          # 页面视图
│   │       └── login/      # 登录注册页
│   └── package.json
├── src/main/java/           # Java 后端代码
│   └── com/example/demo/
│       ├── controller/     # 控制器
│       ├── entity/         # 实体类
│       ├── mapper/         # MyBatis Mapper
│       ├── service/       # 业务逻辑
│       └── common/         # 公共类
├── database/               # 数据库 SQL 脚本
│   ├── testdb.sql         # 主数据库
│   └── sales/            # 销售模块
├── src/main/resources/
│   ├── mapper/           # MyBatis XML 映射文件
│   └── application*.properties  # 配置文件
└── pom.xml               # Maven 配置
```

## 主要功能模块

### 基础数据管理

- 商品管理：商品 SKU、分类、品牌、规格、价格
- 客户管理：客户信息、价格等级、信用额度
- 供应商管理
- 仓库管理
- 商品分类管理

### 库存管理

- 库存查询与预警
- 库存作业（入库、出库、调拨、盘点）
- 库存预警提醒

### 采购管理

- 采购订单
- 采购入库

### 销售管理

- 销售订单
- 销售发货

### 公告系统

- 公告发布与阅读

### 系统管理

- 用户管理
- 角色权限
- 系统菜单
- 操作日志

## 典型使用流程

### 基础数据管理

#### 商品管理

```
新增商品 → 设置分类/价格/规格 → 维护商品信息 → 删除/禁用商品
```

1. **新增商品**：填写商品名称、SKU、分类、品牌、规格、价格等信息
2. **设置分类/价格**：为商品关联分类、设置销售价格和成本价
3. **维护商品信息**：修改商品信息或调整价格
4. **删除/禁用商品**：对不再销售的商品进行禁用或删除

#### 客户管理

```
新增客户 → 设置价格等级/信用额度 → 维护客户信息 → 删除客户
```

1. **新增客户**：填写客户名称、联系方式、地址等信息
2. **设置价格等级/信用额度**：为客户设置对应的价格等级和信用额度
3. **维护客户信息**：修改客户信息
4. **删除客户**：删除不存在的客户

#### 供应商管理

```
新增供应商 → 维护供应商信息 → 删除供应商
```

1. **新增供应商**：填写供应商名称、联系方式、地址等信息
2. **维护供应商信息**：修改供应商信息
3. **删除供应商**：删除不合作的供应商

#### 仓库管理

```
新增仓库 → 设置仓库信息 → 修改仓库状态 → 删除仓库
```

1. **新增仓库**：填写仓库名称、地址、负责人等信息
2. **设置仓库信息**：配置仓库的存储能力和负责人
3. **修改仓库状态**：启用/停用仓库
4. **删除仓库**：删除不再使用的仓库

#### 商品分类管理

```
新增分类 → 构建分类树 → 修改分类 → 删除分类
```

1. **新增分类**：填写分类名称，选择上级分类
2. **构建分类树**：通过上级分类构建多级分类结构
3. **修改分类**：修改分类名称或上级分类
4. **删除分类**：删除空分类

### 库存管理

#### 库存作业

```
新增库存作业 → 执行作业 → 更新作业状态 → 删除作业
```

1. **新增库存作业**：选择作业类型（入库/出库/调拨/盘点），填写商品和数量
2. **执行作业**：根据作业单执行具体操作
3. **更新作业状态**：更新作业执行状态
4. **删除作业**：删除草稿状态的作业

#### 库存预警

```
生成预警 → 查看预警列表 → 确认预警 → 提交补货申请
```

1. **生成预警**：系统扫描所有商品，生成库存预警记录
2. **查看预警列表**：查看低于安全库存的商品预警
3. **确认预警**：对预警进行确认和处理
4. **提交补货申请**：根据预警提交补货申请

### 采购流程

```
创建采购订单 → 确认采购订单 → 生成采购入库单 → 确认采购入库 → 库存增加
```

1. **创建采购订单**：在采购订单页面填写供应商、商品、数量、价格等信息，保存为草稿状态
2. **确认采购订单**：确认后订单状态变为"已确认"，等待入库
3. **生成采购入库单**：选择已确认的采购订单，生成入库单
4. **确认采购入库**：核实货物后确认入库，库存相应增加

### 销售流程

```
创建销售订单 → 确认销售订单 → 生成销售出库单 → 确认销售出库 → 库存减少
```

1. **创建销售订单**：在销售订单页面填写客户、商品、数量、价格等信息，保存为草稿状态
2. **确认销售订单**：确认后订单状态变为"已确认"，等待出库
3. **生成销售出库单**：选择已确认的销售订单，生成出库单
4. **确认销售出库**：核实货物后确认出库，库存相应减少

### 公告系统

```
发布公告 → 查看公告 → 标记已读 → 删除公告
```

1. **发布公告**：填写公告标题、内容，选择发布范围
2. **查看公告**：用户查看公告列表和详情
3. **标记已读**：用户阅读公告后标记为已读
4. **删除公告**：删除过期公告

### 系统管理

#### 用户管理

```
新增用户 → 分配角色 → 维护用户信息 → 禁用/删除用户
```

1. **新增用户**：填写用户名、密码、角色等信息
2. **分配角色**：为用户分配系统角色
3. **维护用户信息**：修改用户信息
4. **禁用/删除用户**：禁用或删除用户账号

#### 操作日志

```
执行操作 → 查看日志 → 导出日志
```

1. **执行操作**：用户执行增删改等操作
2. **查看日志**：在日志页面查看操作记录
3. **导出日志**：导出操作日志用于审计

#### 菜单管理

```
配置菜单 → 设置菜单权限 → 调整菜单顺序
```

1. **配置菜单**：添加/修改系统菜单
2. **设置菜单权限**：为角色配置菜单访问权限
3. **调整菜单顺序**：调整菜单显示顺序

## API 接口

主要接口：`/api`

| 模块 | 接口路径          | 说明     |
| ---- | ----------------- | -------- |
| 登录 | `/api/login`      | 用户登录 |
| 注册 | `/api/register`   | 用户注册 |
| 商品 | `/api/goods/*`    | 商品管理 |
| 客户 | `/api/customer/*` | 客户管理 |
| 库存 | `/api/inventory*` | 库存管理 |
| 销售 | `/api/sale/*`     | 销售订单 |
| 公告 | `/api/notice/*`   | 公告管理 |
| 用户 | `/api/user/*`     | 用户管理 |

## 开发相关命令

```bash
# 后端打包
./mvnw clean package -DskipTests

# 前端构建生产版本
cd Vue && npm run build

# 运行测试
./mvnw test
```

## 注意事项

1. 首次运行需先创建数据库并导入 `database/testdb.sql`
2. 后端端口 8080，前端端口 5173，如遇端口冲突请修改配置文件
3. 生产环境请修改默认密码和 JWT 密钥
4. 前端代理配置在 `Vue/vite.config.js` 中

## 常见问题

**Q: 启动报错数据库连接失败**
A: 检查 MySQL 是否启动，确认数据库用户名密码配置正确

**Q: 前端请求 404**
A: 确认后端服务已启动，检查 Vite 代理配置

**Q: 登录失败**
A: 检查数据库是否正确导入，检查 JWT 配置是否一致
